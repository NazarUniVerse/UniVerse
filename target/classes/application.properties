# ==========================================
# VERİTABANI AYARLARI (Render PostgreSQL)
# ==========================================

# 👇 BURAYA Render'dan aldığın adresi yapıştır.
# DİKKAT: Adresin başı 'jdbc:postgresql://' ile başlamalı!
# Örnek: spring.datasource.url=jdbc:postgresql://kullanici:sifre@adres.render.com/db_ismi
# 1. ADRES (Sadece sunucu adresi ve portu)
spring.datasource.url=jdbc:postgresql://dpg-d5lb27e3jp1c739b0eu0-a:5432/universe_social

# 2. KULLANICI ADI (Ayrı satırda)
spring.datasource.username=universe_social_user

# 3. ŞİFRE (Ayrı satırda)
spring.datasource.password=be06bBPQJJbRqyPGshWMYTahM5thk9px

# PostgreSQL Sürücüsü
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Tabloları Otomatik Güncelle (Veriler SİLİNMEZ, korunur)
spring.jpa.hibernate.ddl-auto=update

# Hata Ayıklama (SQL sorgularını loglarda gösterir)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true